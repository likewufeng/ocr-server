# ReST 印章文字数据集检查报告

> 检查方式：只读检查。原始数据未复制、未改写、未删除。
> 数据集路径：`D:\web\self\study\dataset\ReST`
> 检查结论：**需关注**

## 1. 文件与对应关系

| 项目 | 结果 |
|---|---:|
| 训练图片 | 5000 |
| 测试图片 | 5000 |
| 训练标注条目 | 5000 |
| 标注键范围 | `gt_0` 到 `gt_4999` |
| 缺失标注键 | 0 |
| 有图片无标注 | 0 |
| 有标注无图片 | 0 |
| 训练图片无法读取 | 0 |
| 训练图片压缩包 | 存在 |
| 测试图片压缩包 | 存在 |

当前训练集文件名和标注可以完整对应。测试集有图片但没有公开金标准文字标注，因此不能直接计算测试集准确率。

## 2. 标注统计

| 项目 | 结果 |
|---|---:|
| 文字实例数 | 5000 |
| 每张图片平均实例数 | 1.00 |
| 空标注条目 | 0 |
| 空转写文本 | 0 |
| 含替换字符 `�` 的转写 | 0 |
| 不同转写数量 | 2943 |
| 文本长度最小/最大/平均 | 1 / 29 / 12.06 |
| polygon 点数最小/最大/平均 | 0 / 1643 / 361.72 |
| polygon 点数少于 3 的实例 | 114 |

文本长度分布 Top 10：9字符：1160条、13字符：704条、10字符：655条、12字符：520条、14字符：429条、11字符：321条、15字符：285条、17字符：145条、16字符：142条、19字符：121条

## 3. 图片统计

主要图片尺寸：285x185（579张）、300x300（455张）、180x180（164张）、220x220（148张）、260x260（146张）、280x280（139张）、240x240（137张）、200x200（132张）、245x163（7张）、240x155（6张）

## 4. 标注格式说明

标注文件是 JSON 对象，键名为 `gt_N`，对应 `ReST_train/train_images/N.jpg`。每个值是文字实例数组，实例包含：

- `points`：文字区域 polygon，点数不固定，不能按四边形处理；
- `transcription`：该区域的文字转写。

训练集当前每张图片恰好一个文字实例。原始 polygon 可能有很多点，做检测训练转换时应原样保留，或在转换到矩形检测格式时明确记录近似损失。当前有 114 个实例的 `points` 少于 3 个点，不能作为有效检测 polygon；做检测训练前应过滤，并保留这些样本用于识别训练或质量排查。

## 5. 字符表与文本风险

`chars.txt` 大小为 26780 字节，读取字符数为 13464，其中替换字符 `�` 数量为 0。

前 10 条转写抽样：
- `'北川羌族自治县永安镇长青村'`
- `'武汉振安科技有限公司'`
- `'北京华信标研检测技术服务有限公司'`
- `'贵州德睿资产管理有限公司'`
- `'北川羌族自治县永安镇工农村'`
- `'全国统一发票监制章'`
- `'贵州务川宝达置业有限公司'`
- `'重庆和辅广告设计有限公司'`
- `'四川鑫泰物业服务有限公司'`
- `'深圳市科定木饰面科技有限公司'`

如果抽样或统计中出现大量 `�`，应先确认数据发布包本身是否把不可辨识字符编码为替换符。此类字符不能恢复为真实汉字，不应把它们当作可纠正的 OCR 答案；训练前建议按项目策略过滤、映射为特殊占位符，或保留为“不可识别字符”样本并单独统计。

## 6. 后续使用建议

1. 原始 ReST 目录作为只读归档，不直接复制到项目仓库。
2. 先用训练集划分出固定验证集，例如按图片编号固定保留 10%，不要每次随机变化。
3. 保留 ReST 原始 JSON，同时生成项目自己的 JSONL manifest，记录 `image`、`points`、`text`、`split` 和 `dataset`。
4. 先做当前 PP-OCRv6 基线，再做 ReST 专用识别模型微调，对比字符准确率、整串准确率和圆章真实样本效果。
5. ReST 官方许可仅限非商业研究和教育用途；未取得商业授权前，不应把其图片、标注或训练产物直接用于生产商业服务。
