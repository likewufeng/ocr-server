services:
  paddle-ocr:
    volumes:
      - ./:/app
    environment:
      WATCHFILES_FORCE_POLLING: "true"
    command: >-
      uvicorn app.main:app --host 0.0.0.0 --port 8000
      --reload --reload-dir /app/app --reload-include "*.py"
